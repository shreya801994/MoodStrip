initCamera();

window.startSession = startSession;
window.generateStrip = generateStrip;
window.resetSession = resetSession;
window.generateQR = generateQR;
window.setTheme = setTheme;
window.downloadStrip = downloadStrip;